#include <iostream>

using namespace std;

void solution() {
    long long a, b, c;
    cin >> a >> b >> c;
    cout << a+b+c;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    solution();

    return 0;
}