#include <iostream>
#include <cmath>

using namespace std;

void solution() {
    int N;

    cin >> N;
    cout << sqrt(N*2)*4;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    solution();
}