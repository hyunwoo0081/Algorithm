#include <iostream>

using namespace std;

void solution() {
    int B;
    cin >> B;
    B = B * 10 / 11;
    cout << B;
}

int main() {
    ios_base :: sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    solution();
}