#include <iostream>
#include <algorithm>

using namespace std;

void solution() {
  int A, B;
  cin >> A >> B;
  cout << A + B + 3;
}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  solution();
}