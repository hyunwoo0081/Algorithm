#include <iostream>

using namespace std;

void solution() {
    long long N;
    cin >> N;
    cout << N*N*N << '\n' << 3;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    solution();

    return 0;
}