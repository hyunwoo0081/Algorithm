#include <iostream>

using namespace std;

void solution() {
    int n;
    cin >> n;
    cout << 4000 * n;
}

int main() {
    ios_base :: sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    solution();
}