#include <iostream>

using namespace std;

int main() {
    string a, b;
    
    cin >> a >> b;
    if (a == b) {
        cout << 0;
    }
    else {
        cout << 1550;
    }
    
    return 0;
}